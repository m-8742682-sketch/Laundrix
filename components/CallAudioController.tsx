/**
 * CallAudioController — RETIRED
 * Sound is now handled entirely by GlobalSoundController in _layout.tsx.
 * This component is kept as a no-op so existing imports don't break.
 */
export default function CallAudioController() {
  return null;
}